# FEAT-ALL-THEATRE-COORDS-001 — Vendor dcs-maps.yaml + data-drive coordinates.py

Status: ✅ done
Type: feat
Files: `src/python/veaf-tools/veaf_libs/data/dcs-maps.yaml`, `veaf_libs/coordinates.py`, `test/python/veaf_libs/test_coordinates.py`

## What was built

- Vendored `exports/maps.yaml` from VEAF/dcs-maps (MIT) as `veaf_libs/data/dcs-maps.yaml` (verbatim,
  keeps the upstream `# Generated by …` header for drift comparison).
- `coordinates.py` now loads `lon_0`/`x_0`/`y_0` per theatre from it (cached `_load()`), keyed by the
  lowercased DCS theatre string, with `_THEATRE_ALIASES` (`sinai`→`sinaimap`,
  `germanycoldwar`→`germanycw`). Pure-Python TM + `offset_latlon` unchanged (no `pyproj`).
- `supported_theatres()` returns the DCS-spelled names (14).

## Acceptance criteria

- [x] The 5 `projection.lua` reference cases still pass (`< 5e-6°`, `< 0.5 m` round-trip).
- [x] `supported_theatres()` ≥ 14; includes Normandy/Nevada/SinaiMap/GermanyCW.
- [x] Aliases resolve (`Sinai` ≡ `SinaiMap`); unknown theatre raises naming it.
- [x] ruff + mypy clean.
