# FEAT-MCP-PLUGIN-003 — Install doc + optional marketplace

Status: ⬜ ready
Type: docs
Files: `doc/`, `.claude-plugin/marketplace.json` (optional), `CHANGELOG.md`

## What to build

- Document how a Mission Maker installs the plugin from this repo (and configures the MCP server).
- Optionally add a `marketplace.json` so `claude plugin marketplace add VEAF/VEAF-Mission-Creation-Tools`
  works; and/or coordinate with BFR to list it in the `bfr` marketplace via an external `source`.
- CHANGELOG.

## Blocked by

FEAT-MCP-PLUGIN-002.
