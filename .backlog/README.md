# Backlog — VEAF Mission Creation Tools v6

Per-lot backlog. Active lots are directories under `.backlog/<LOT-ID>/`; completed
lots are compacted into `.backlog/archive/<LOT-ID>.md`. Sequencing lives in
[ROADMAP](../ROADMAP.md); this index is the source of truth for **scope and status**.

## Legend

- **Status**: ⬜ ready · 🔄 in-progress · 🧑 waiting-human · ✅ done · 🚫 wontfix

## Active lots

| Lot | Status |
|-----|--------|
| [FIX-SERVERHOOK-UNKNOWN-PILOT-PARSE](FIX-SERVERHOOK-UNKNOWN-PILOT-PARSE/PRD.md) — on "privé 2" no pilot was recognized (admin included) and any `/command` crashed the hook, surfaced once #590 revived the chat callback. Root cause: the shared `veaf-pilots.txt` lives in the `Saved Games\` root but `loadPilots` looked under the per-server `scripts\hooks\` (`no file …` on every load) → default the path to the shared root (`writedir()\..\`, `VEAF_SHARED_DIR`). Plus two guards: `loadPilots` loads defensively instead of `assert`-crashing (dead-code error branch), and `parse` gives an unlisted pilot `level = -1` instead of indexing a `nil` pilot (`:413`). Lua-only, no product bump; manual redeploy. Reported by David | 🔄 |
| [FIX-WORKFLOWS-MAIN-TO-MASTER](FIX-WORKFLOWS-MAIN-TO-MASTER/PRD.md) — every CI workflow triggered on a `main` branch that does not exist here (stable branch is `master`), so no CI ran on `master` pushes — quality/SBOM/secret-scanning/`latest`-docs all dead on the branch that receives release merges. Mechanical `main` → `master` rename across 7 workflows (`develop-v6` triggers + `v*` tag path untouched). CI-only, no product bump. Surfaced after the v6.10.0 release | ✅ |
| [FIX-SECRET-SCANNING-GITLEAKS-CLI](FIX-SECRET-SCANNING-GITLEAKS-CLI/PRD.md) — the `Secret Scanning` workflow used `gitleaks/gitleaks-action@v3`, whose wrapper needs a **paid licence for GitHub orgs** (`GITLEAKS_LICENSE`); unset, so the job failed on every run since 2026-05-20 and secret scanning never ran. Swap the wrapper for the free MIT-licensed **gitleaks CLI** (pinned 8.30.1, `gitleaks git --config .gitleaks.toml`). CI-only, no product bump. Surfaced during the v6.10.0 release | ✅ |
| [REFACTOR-SERVER-HOOK-CANONICAL](REFACTOR-SERVER-HOOK-CANONICAL/PRD.md) — make the repo `VEAF-Server-hook.lua` the single deployable source again: the 6 VEAF servers run a hand-edited **allégée** variant (no BufferingSocket, autorestart commented, shared pilots path) that diverges from the repo, and the repo's hard `require('BufferingSocket')` **crashes the hook at load** when the native module is absent. Turn the divergences into OFF-by-default flags (`enableBufferingSocket` via defensive `pcall`, `enableAutoRestart`, `pilotsDir`) set by the per-server specific-hook — repo defaults reproduce prod behaviour, so deploying becomes a copy-paste. Validated via `luac -p` + a mock smoke test. Follows FIX-SERVERHOOK-CHAT-SIM-LOGGER | ✅ |
| [FIX-SERVERHOOK-CHAT-SIM-LOGGER](FIX-SERVERHOOK-CHAT-SIM-LOGGER/PRD.md) — two runtime defects hit on the "privé 2" server (DCSServerBot active): `veaf.Logger:print` indexes the GameGUI-only global `Sim` (`Sim.getMissionName()`) which is nil in the mission env, crashing **every** `:error()` (swallows carrier-ops player messages, breaks unrelated error paths) → use `veaf.config.MISSION_NAME`; and `VEAF-Server-hook.lua` listens on the non-existent `onChatMessage` callback instead of `onPlayerTrySendChat`, so no server chat command (`/secu login`, `/send`, …) has ever run → rename + remap params + fix the return contract. Reported by David | ✅ |
| [FEAT-COMBATZONE-RADIO-GROUPS](FEAT-COMBATZONE-RADIO-GROUPS/PRD.md) — two optional per-zone combat-zone keys `radio_group_name` (gathers zones under a named submenu) + `radio_menu_prefix` (label prefix), both 1:1 onto the orphan runtime setters and round-tripped by `convert-v5`; plus **automatic render-time radio menu pagination** ([ADR 0013](../docs/adr/0013-radio-menu-pagination.md)) in `RadioMenuBuilder:_buildSubtree` (opt-out via `veafRadio.doNotPaginate`, exact per-node count, `ForUnit` auto-disable) so every menu — Combat Zone included, for free — stops overflowing the DCS 10-item limit | ✅ |
| [FEAT-MCP-MISSION-EDITOR](FEAT-MCP-MISSION-EDITOR/PRD.md) — first phase of NL-MISSION-GEN: MCP server for LLM-assisted mission editing. v1 exposes editor-parity actions that mutate the mission's source `.miz` in place (bypassing `mission.yaml`), backed up before every write — a read action wrapping the existing `export` JSON contract, and a write action `add_group` (order of battle first). Waves 1-12 done (wave 9 = folder scaffolding #581; wave 10 = map + coordinates #583, ADR 0015; wave 11 = build + validate #585; wave 12 = optional unit name → `#command` combat-zone spawn idiom #586). MCP spans create→edit→validate→build→play (28 actions). Umbrella PR #575 → `develop-v6` held open until the MCP is considered complete (packaging as a Claude plugin still pending). Extends [ADR 0014](../docs/adr/0014-mission-editor-mcp-editor-parity-layer.md) | ✅ |
| [FEAT-MCP-PLUGIN](FEAT-MCP-PLUGIN/PRD.md) — ship `veaf-mission-mcp` + the authoring skill as a **self-hosted Claude plugin** (in this repo, not bfr-claude-plugins — a marketplace can reference it externally). 001–003 done: `veaf-tools mcp` subcommand embeds the server in the shipped binary; the plugin manifest + `.mcp.json` + `marketplace.json` + a `SessionStart` bootstrap (`bootstrap.ps1`, Windows) that auto-installs/refreshes `veaf-tools` via `veaf-tools-updater` (throttled 4h, `VEAF_MCP_UPDATER_TAG` to target a pre-release); install doc (`doc/mission-maker/AI_ASSISTANT_INSTALL.md`, FR+EN). Installable + self-updating from this repo | ✅ |
| [FEAT-MCP-ORACLE-COMMANDS](FEAT-MCP-ORACLE-COMMANDS/PRD.md) — close the oracle gap behind real-usage feedback (LLM invented `-lrsam` for a SAM instead of the real `-samLR`): `list_shortcuts` now also exposes the 128 `#command` aliases from `veafShortcuts.buildDefaultList()` (new `commands` block, scanner + build-time JSON generation à la `lua_module_scanner`); collateral fix = bundle the missing `dcsUnits.yaml` into the binary so `list_unit_types` works in the shipped plugin; skill steers to `list_shortcuts` first (PR #592) | ✅ |
| [FIX-MCP-SCAFFOLD-THEATRE-HINT](FIX-MCP-SCAFFOLD-THEATRE-HINT/PRD.md) — real-usage fix: the assistant scaffolded a Syria mission without `theatre` (empty `src/mission`) then wrongly concluded the scaffold failed. `scaffold_mission`'s action + `theatre` descriptions now steer the LLM to pass `theatre` for a supported map (the skill already did), and `theatre` gains an `enum` from `supported_theatres()`. Collateral bundling fix: `theatre-defaults.yaml` / `dcs-maps.yaml` / `theatre-bounds.yaml` were missing from the binary → all map/geo/blank actions broke in the shipped plugin; now bundled (same as `dcsUnits.yaml`) — the real prerequisite for the dev-release | ✅ |
| [FIX-RELEASE-WORKFLOW-PRERELEASE](FIX-RELEASE-WORKFLOW-PRERELEASE/PRD.md) — a `publish --prerelease` still shipped the dev branch to all production makers: the CI `release.yml` republished as latest and advanced `published-latest` unconditionally, ignoring the flag. Pre-release is now keyed off a semver suffix in the version (`published-v6.9.21-rc1`) — `github.py._is_prerelease` detects it, the workflow skips every `published-latest` step, and the CLI rejects `--prerelease` on a plain version. (`published-latest` restored to 6.9.2 operationally.) | ✅ |
| [FIX-UPDATER-PAUSE-HANG](FIX-UPDATER-PAUSE-HANG/PRD.md) — plugin bootstrap / `scaffold_mission` hung (two updaters at 0 % CPU): `veaf-tools-updater`'s exit `input()` pause fired when launched from the hidden-window SessionStart hook and blocked forever, keeping its exe locked. Updater now honours `VEAF_UPDATER_NO_PAUSE` (set by `bootstrap.ps1` + `scaffold_mission`); `scaffold._run` closes stdin + bounds a timeout so a stalled child fails fast. Not `prepare` (which is `--force` + TTY-guarded) | ✅ |
| [FIX-LONG-FILENAMES-WINDOWS](FIX-LONG-FILENAMES-WINDOWS/PRD.md) — real-usage (clean Windows VM): the plugin marketplace clone failed on "Filename too long" — 42 empty KNEEBOARD fixture placeholders with 95-char names blew past Windows' 260-char MAX_PATH under a deep clone path, so `plugin install` never found the plugin. Renamed them to `.gitkeep` (folders kept, longest tracked path now 91); install doc notes `core.longpaths`. Fixture/docs only | ✅ |
| [FEAT-MCP-ADD-GROUP-FOLDER](FEAT-MCP-ADD-GROUP-FOLDER/PRD.md) — real-usage fix: a permanent SAM (`#veafInterpreter["-samLR"]` carrier) could only be written to the built `.miz` (transient) because `add_group` was `.miz`-only, while `scaffold --theatre` lays down an exploded `src/mission/`. `add_group` is now **folder-aware** (`target` = folder → durable recipe, or `.miz` → transient), reusing the core already shared with the composites; result gains `durable`. Skill updated | ✅ |
| [FIX-MCP-STDOUT-POLLUTION](FIX-MCP-STDOUT-POLLUTION/PRD.md) — real-usage (clean env / Claude Desktop): the MCP server showed up **connected but with no tools**. Cause: it logged `Starting…` via the Rich console (→ stdout), corrupting the JSON-RPC stdio handshake. New `Logger.mute_console()` called in `server.main()` before `mcp.run()` → logs on stderr/file only, stdout is JSON-RPC only. Verified stdout empty at startup | ✅ |
| [FIX-MCP-INTERPRETER-DOC](FIX-MCP-INTERPRETER-DOC/PRD.md) — real-usage fix: for a permanent SAM the assistant left the MCP to look up `#veafInterpreter`'s format in the external BFR plugin. The format we expose was already correct (`veafInterpreter.lua`); the skill just mentioned it in one line. Now it has a worked example (`#veafInterpreter["-samLR"]` → permanent SAM at start) + the permanent-vs-combat-zone distinction, so the MCP is self-sufficient. Docs only | ✅ |
| [FEAT-MCP-AIRBASES-WAREHOUSES](FEAT-MCP-AIRBASES-WAREHOUSES/PRD.md) — real-usage: a base stayed neutral when assigned a coalition, and Dynamic Slots were skipped (`no warehouse airports`). New MCP action `set_airbase_coalition` writes `warehouses.airports[<id>].coalition` durably (lazy entry, name→id via theatre) and enables the base's Dynamic Spawn slots; the warehouses injector auto-fills a coalition's bases from the injected dynamic templates when no explicit `aircrafts:` list is given, and the default `warehouses.yaml` is now effective. Plus alias-first skill guidance + a structured `category` on `list_shortcuts` | ✅ |
| [FIX-MCP-TEST-FEEDBACK](FIX-MCP-TEST-FEEDBACK/PRD.md) — two real-usage fixes: (1) skill now states a combat-zone `#command` fake-unit spawns in **its own coalition** (`veafInterpreter` uses `unit:getCoalition()`) — the assistant wrongly thought `-samLR` was always red; (2) `scaffold_mission` defaults its `tag` to `VEAF_MCP_UPDATER_TAG` so it installs the MCP's own version (e.g. a pre-release with `--theatre`) instead of the stale `published-latest` 6.9.2 that made `prepare --theatre` exit 2 | ✅ |
| [FEAT-ALL-THEATRE-COORDS](FEAT-ALL-THEATRE-COORDS/PRD.md) — coordinate conversion for **all DCS theatres**: `coordinates.py` is now data-driven from a vendored export of VEAF/dcs-maps (`data/dcs-maps.yaml`, MIT — Mitch), keeping our pure-Python Transverse Mercator (no pyproj); adopting the `dcs-maps-coordinates` package was rejected (native pyproj/mgrs, PyInstaller-hostile). 14 theatres, exact DCS keys + aliases. See [ADR 0015](../docs/adr/0015-coordinate-projection-port.md) | ✅ |
| [FEAT-GEO-PLACEMENT](FEAT-GEO-PLACEMENT/PRD.md) — place things by real-world geography (DCS theatres are the real world projected; wave-10 `coordinates` bridges DCS x/y ↔ lat/lon). Pluggable geocoder (OSM Nominatim default, Google optional) resolves a place name → lat/lon → DCS x/y, + a geodesic offset helper for "N km bearing from X", exposed as a `geocode` MCP action with per-theatre bounds. Named places/POIs + relative offsets in scope; vague unnamed terrain and metre-accuracy out. Implemented (001-005) | ✅ |
| [FEAT-BLANK-MISSION-THEATRE](FEAT-BLANK-MISSION-THEATRE/PRD.md) — synthesize a minimal loadable blank mission per theatre in Python (generic skeleton + per-theatre constants: name, bullseye, map centre — data, not vendored `.miz`), wired into `prepare --theatre <name>` (shared primitive) with `scaffold_mission` forwarding a `theatre` param. Removes the DCS round-trip to start a mission. Caucasus DCS-verified (PR #582); extended to **9 theatres** by extracting map-centre + bullseye from VEAF/dcs-maps `data/maps/*.miz` calibration missions (parsed with read_miz — real values, .miz not vendored) | ✅ |
| [FEAT-THIRD-PARTY-MODS](FEAT-THIRD-PARTY-MODS/PRD.md) — port the v5 per-mission `build.cmd` hack into the v6 build: strip selected third-party aircraft mods from the `.miz`'s `requiredModules` so a pilot without the paid/community mod can still load the mission (the slot is just unavailable). Data-driven — a bundled VEAF default list, unioned with an overridable `mission.third_party_mods` field | ✅ |
| [FEAT-PRESETS-PRIORITY-COLOR](FEAT-PRESETS-PRIORITY-COLOR/PRD.md) — channel `priority` (universal kneeboard highlight; consumed by the AJS-37 layout to fill FR22 Special 1/2/3 + FR24 H) & `color` (kneeboard CH-cell grouping); AJS-37 key-based Group 100–139 packer with Group-100 recycle + `{priority}` specials; per-type kneeboards in `KNEEBOARD/<type>/IMAGES/` with grey headers + Viggen pilot labels. Extends [ADR 0012](../docs/adr/0012-channel-priority-colour-and-ajs37-packing.md); breaks 0003 iso-functionality for the AJS-37. Driver: Viggen (Tripack). convert-v5 out of scope | ✅ |
| [FIX-CONVERTV5-PRESETS-OUTPUT](FIX-CONVERTV5-PRESETS-OUTPUT/PRD.md) — cleaner generated `presets.yaml` (David's test feedback): uniform **integer** channel keys (was `'01'` strings in `channel_lists` vs `12` ints in overrides, with PyYAML octal-quoting `'01'` but not `08`), and **header comments** on the plan + faithful files (`_yaml_dump` gains an optional header) | ✅ |
| [FEAT-AIRFIELD-FREQS-DATA](FEAT-AIRFIELD-FREQS-DATA/PRD.md) — lot 2/3 of the convert-v5 preset-aliasing plan: extend `veaf-build update-dcs-data` to parse `Mods/terrains/<Theatre>/Radio.lua` into a bundled, versioned `airfield-frequencies.yaml` (`theatre → airfield → {uhf,vhf,fm}`), the data source for the freq→airfield reverse-lookup. Prerequisite for FEAT-CONVERTV5-FREQ-ALIASING | ✅ |
| [FEAT-CONVERTV5-FREQ-ALIASING](FEAT-CONVERTV5-FREQ-ALIASING/PRD.md) — lot 3/3 (extends ADR 0010): `convert-v5` replaces hardcoded preset frequencies with readable aliases — airfields (`Gudauta`, from lot 2) and VEAF flight/tactical conventions (`Archer`, `Guard`…) — via a `freq+band → alias` reverse-lookup, inserting the catalog into both output files; unmatched freqs stay raw. Includes the aircraft type-alias annex (`AH-64D → AH-64D_BLK_II`). Needs lot 2 | ✅ |
| [FEAT-RADIO-YAML-MENUS](FEAT-RADIO-YAML-MENUS/PRD.md) — declare F10 radio menus in YAML without Lua ([ADR 0011](../docs/adr/0011-radio-yaml-menus.md)). Two mechanisms sharing one action vocabulary (`qra.*`, `airwave.*`, `flag.*`, `message`, `lua`): a per-module `radio_menu` shortcut (QRA + AirWaves — the only triggerable subsystems with no standard menu) and a free `modules.RADIO.user_menus` Mission-Master tree, both optionally restricted to a DCS group. `lua` action references a maker function (build fails if absent). Reported by Tripack | ✅ |
| [DOC-TRIPACK-FEEDBACK](DOC-TRIPACK-FEEDBACK/PRD.md) — doc-only (direct commits): document the auto-managed carrier Pedro (rescue helo) & S3B-Tanker groups (naming convention `<carrier> Pedro` / `<carrier> S3B-Tanker`) in `veafCarrierOperations.md`; fix the stale `mission.yaml → qra:` reference in `veafQraManager.md` (QRA-in-YAML already works via `modules.QRA`). Reported by Tripack | ✅ |
| [FEAT-PRESETS-KNEEBOARD-TOGGLE](FEAT-PRESETS-KNEEBOARD-TOGGLE/PRD.md) — make `pipeline.presets` polymorphic (scalar *or* `{enabled, kneeboards}`) so kneeboard PNG generation can be disabled globally while keeping radio injection; add a discoverable "Configurer le pipeline" section to the MM GUIDE. Per-plate selection dropped. Reported by Tripack + David | ✅ |
| [FIX-MISSILEGUARDIAN-INIT-CRASH](FIX-MISSILEGUARDIAN-INIT-CRASH/PRD.md) — `MISSILEGUARDIAN: true` crashed VEAF start-up (`veafMissileGuardian.initialize()` called the never-defined `dumpMissionsList`), aborting `veaf-config.lua` and silently disabling the central F10 marker dispatcher (dead `_spawn`/shortcut aliases despite `SHORTCUTS: true`), plus CTLD/CSAR init. Also stopped auto-enabling the module (2021 WIP relic): removed from the `full` tier → opt-in only. Reported by Tripack | ✅ |
| [ENRICH-PREPARE-TEMPLATE](ENRICH-PREPARE-TEMPLATE/PRD.md) — `prepare --template` now emits the same rich `mission.yaml` preamble as `convert-v5` (YAML guide, `global_log_level`, `mission:`, `security:`, `pipeline:`) via shared helpers; `generate-config` output unchanged. Reported by Tripack | ✅ |
| [FIX-MIG15-PRIMARY-FREQ](FIX-MIG15-PRIMARY-FREQ/PRD.md) — build wrongly rejects the MiG-15bis HF primary frequency (RSI-6K 3.75 MHz, below the 30 MHz floor); the safety net is now spec-aware (`is_strict` + in-spec) while still catching ADF promotion (Yak-52 0.625 MHz). Reported by Tripack | ✅ |
| [FIX-EVENTHANDLER-UNITCATEGORY](FIX-EVENTHANDLER-UNITCATEGORY/PRD.md) — dynamic-slot airplane still ignored by the QRA unless `react_on_helicopters` true: `completeUnitFromName` populated `unitCategory` with `getCategory()` (Object.Category UNIT=1 collides with HELICOPTER), bypassing the #299 fix | ✅ |
| [FEAT-LUA-BUILD-STAMP](FEAT-LUA-BUILD-STAMP/PRD.md) — single build stamp (`6.7.x+<sha>`) in the DCS log instead of 33 unreliable per-module versions; retires the hand-maintained `.Version` constants | ✅ |
| [FIX-CTLD-REPACK-NIL-GROUP](FIX-CTLD-REPACK-NIL-GROUP/PRD.md) — CTLD F10 menu duplicated on dynamic-slot helo on a runtime FARP (`getUnitsInRepackRadius` nil `getGroup`) + analysis doc for the CTLD rewrite | ✅ |
| [FIX-QRA-DYNSLOT-CATEGORY](FIX-QRA-DYNSLOT-CATEGORY/PRD.md) — QRA triggers on dynamic-slot airplanes regardless of `react_on_helicopters` (#299: `getCategory`→`getCategoryEx`) | ✅ |
| [UX-PLURAL-SWEEP](UX-PLURAL-SWEEP/PRD.md) — natural singular/plural (`tn`) across all ~40 count-bearing CLI messages (FR/EN) | ✅ |
| [UX-PIPELINE-OUTPUT-POLISH](UX-PIPELINE-OUTPUT-POLISH/PRD.md) — `build` pipeline output: indent detail lines under each step + natural singular/plural (`tn`) | ✅ |
| [UX-AIRCRAFT-SKIPPED-REPORT](UX-AIRCRAFT-SKIPPED-REPORT/PRD.md) — `build` names the spawn-data file + reports aircraft skipped because already present | ✅ |
| [FIX-CONVERT-SPAWNABLES-FLAT-FORMAT](FIX-CONVERT-SPAWNABLES-FLAT-FORMAT/PRD.md) — `convert-v5` converts spawnable aircraft from the flat `settings.lua` layout (was: empty `spawnables.yaml`) | ✅ |
| [FEAT-MIGRATE-MISSION-V6](FEAT-MIGRATE-MISSION-V6/PRD.md) — promote `src/mission/` from v5 to v6 on disk | ✅ |
| [FIX-DYNSLOT-TEMPLATE-CATEGORY](FIX-DYNSLOT-TEMPLATE-CATEGORY/PRD.md) — airplane dynamic-slot templates miscategorized as helicopters | ✅ |
| [FEAT-RADIO-PRESET-PROJECTION](FEAT-RADIO-PRESET-PROJECTION/PRD.md) — per-type radio-preset projection (preset plan model): role-based `channel_lists` + VEAF `dcs-radio-layouts.yaml` + generator-overlay packer; convert-v5 generates a plan by default (ADR 0010). Phase 1 core + phase 2 convert-v5 | ✅ |
| [ENRICH-DEFAULT-PRESETS](ENRICH-DEFAULT-PRESETS/PRD.md) — broaden the shipped default radio presets (fold into / sequence after FEAT-RADIO-PRESET-PROJECTION phase 1) | ⬜ |
| [FEAT-EXPORT-BFR-PARSER](FEAT-EXPORT-BFR-PARSER/PRD.md) — `export` as the safe mission parser for the BFR plugin (`.miz`/folder, JSON array-ness contract) | ✅ |
| [TOOLING-DCS-MOCK-COVERAGE](TOOLING-DCS-MOCK-COVERAGE/PRD.md) — audit DCS-mock coverage against a vendored API schema | ✅ |
| [FIX-DCS-MOCKS-COMPLETION](FIX-DCS-MOCKS-COMPLETION/PRD.md) — fill the 10 DCS-mock gaps surfaced by `audit-dcs-mocks` | ✅ |
| [VENDORED-DRIFT-WATCH](VENDORED-DRIFT-WATCH/PRD.md) — scheduled drift-watch (manifest + check + cron→issue) for all vendored artifacts | ✅ |
| [FIX-CONVERTV5-ICAO-MESSAGE](FIX-CONVERTV5-ICAO-MESSAGE/PRD.md) — reword the `convert-v5` empty-ICAO notice (conversion succeeded + lightest fix first) | ✅ |
| [FIX-V5-NUDGE-FALSE-POSITIVE](FIX-V5-NUDGE-FALSE-POSITIVE/PRD.md) — stop the migrate_from_v5 nudge firing on already-promoted v6 missions (key discriminant) | ✅ |
| [FIX-CLI-UTF8-ASK-STREAMING](FIX-CLI-UTF8-ASK-STREAMING/PRD.md) — UTF-8 stdout (stop truncating `ask`/reports on Windows) + live `ask` streaming | ✅ |
| [FIX-BUILD-PROFILES](FIX-BUILD-PROFILES/PRD.md) — case-insensitive `--profile` (canonical name) + orphan warning gated on the base+profiles union | ✅ |
| [CONVERT-V5-UX](CONVERT-V5-UX/PRD.md) — triage leftover v5 files (tooling→backup, regenerable→delete, unrecognized→inform) + drop the misleading annotated-missionConfig report block + remove the leftover "missionConfig.lua edits" report noise | ✅ |
| [DOC-GUIDE-ANCHORS](DOC-GUIDE-ANCHORS/PRD.md) — fix the `mission.yaml` `# Doc:` deep links (trailing slash + stable explicit FR/EN anchors via `attr_list`) | ✅ |
| [FIX-CONVERT-WEATHER-I18N](FIX-CONVERT-WEATHER-I18N/PRD.md) — route 3 hardcoded-English convert-v5 weather/waypoints warnings through `t()` (FR/EN) | ✅ |
| [FIX-CLEANUP-EXCLUDE-TOOLCHAIN](FIX-CLEANUP-EXCLUDE-TOOLCHAIN/PRD.md) — stop the convert-v5 cleanup from listing `veaf-tools*.exe` as deletable "unrecognized" files | ✅ |
| [FEAT-CROSSPLATFORM-BINARIES](FEAT-CROSSPLATFORM-BINARIES/PRD.md) — ship standalone `veaf-tools` Linux/macOS binaries as extra release assets (per-OS CI jobs; Windows flow unchanged) + expose `veaf-tools.exe` as a direct asset | ✅ |
| [UPDATER-CROSSPLATFORM](UPDATER-CROSSPLATFORM/PRD.md) — port `veaf-tools-updater` to Linux/macOS (download per-OS binary assets, `chmod +x`, direct self-update; Windows path unchanged) | ✅ |
| [RELEASE](RELEASE/PRD.md) — v6.10.0 release: first official stable v6 cut to `master` (bump 6.9.30 → 6.10.0, `merge -s ours` of the v5 line then ff onto v6, tags `published-v6.10.0` + `v6.10.0`) | ✅ |
| [FIX-PYINSTALLER-RADIO-LAYOUT-DATA](FIX-PYINSTALLER-RADIO-LAYOUT-DATA/PRD.md) — `veaf-tools.spec` was missing `dcs-radio-layouts.yaml` in `datas`, breaking `convert-v5` radio-preset conversion in the packaged `.exe` only. Reported by David | ✅ |
| [FIX-VEAF-BUILD-RADIO-LAYOUT-DATA](FIX-VEAF-BUILD-RADIO-LAYOUT-DATA/PRD.md) — the real fix: `veaf-tools.spec` is dead code, the actual build pipeline (`veaf_build/worker.py`) had the same missing `dcs-radio-layouts.yaml` entry. Reported by David | ✅ |
| [FEAT-CONVERTV5-PLAN-PRESETS](FEAT-CONVERTV5-PLAN-PRESETS/PRD.md) — convert-v5 emits two preset files: `presets.yaml` (simplified plan exploiting `channel_lists`, default/loaded) + `presets.v5.yaml` (faithful iso-functional copy, reference). Lets the maker actually use the crystallisation. Reported by David (ADR 0010) | ✅ |

## Archived lots

See [`archive/`](archive/). Index rows are added here as lots are archived.
