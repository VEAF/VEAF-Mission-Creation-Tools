# DCS Dynamic-Slot template groups (`dynSpawnTemplate = true`). Empty by default —
# a fresh mission ships no dynamic-slot templates. Populate with
# `veaf-tools extract-aircraft-groups` (or it is produced by `convert-v5`) from a
# mission that actually contains dynamic-slot template groups.
airplanes:
  coalitions: {}
helicopters:
  coalitions: {}
